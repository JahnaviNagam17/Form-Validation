# 🧾 Form Validation using Streamlit

This project is a simple **Form Validation Web App** built using **Python** and **Streamlit**.  
It takes user inputs such as **Name**, **Date of Birth (DOB)**, **Email**, and **Mobile Number**,  
and validates each field using **Regular Expressions (RegEx)** to ensure the data format is correct.

---

## 🚀 Features

✅ Validates **Name** (must start with a letter and can contain letters or numbers)  
✅ Validates **Date of Birth (DOB)** in **MM-DD-YYYY** format  
✅ Validates **Email ID** format (e.g., example@gmail.com)  
✅ Validates **Mobile Number** (10 digits only)  
✅ Displays **Success** message if all fields are valid  
✅ Displays specific **Warning messages** if any field is invalid  

---

## 🧠 Technologies Used

- **Python 3**
- **Streamlit** – for building the web interface  
- **re (Regular Expressions)** – for validating user input formats  

---

## 📦 Installation and Setup

Follow the steps below to run the project locally:

1. **Clone this repository**
   ```bash
   git clone https://github.com/<your-username>/<repository-name>.git
   cd <repository-name>
